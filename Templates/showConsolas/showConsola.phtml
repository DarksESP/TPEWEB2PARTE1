<div>
  <p><?php $mensaje?></p>
</div>

<?php
if (session_status() === PHP_SESSION_NONE) {
    session_start();
}
 if (isset($_SESSION['ID_USER'])) : ?>
    <div class="container mb-4 d-flex justify-content-end">
        <a href="<?= BASE_URL ?>agregar-consola" class="btn btn-warning btn-lg shadow-sm">
            <i class="bi bi-plus-circle me-2"></i> Agregar Consola
        </a>
    </div>

    <?php endif; ?>
  ?>

<div class="container my-4">
    <div class="row row-cols-2 row-cols-sm-3 row-cols-md-4 g-3">
            <div  class="col">
              <a href="<?= BASE_URL . 'juegos-categoria/' . $consola->id ?>" class="col text-decoration-none">
                <section class="card h-100 text-center shadow-sm" style="font-size: 0.9rem;">
                    <!-- Imagen de la consola -->
                    <?php if (!empty($consola->imagen)): ?>
                        <img src="<?= BASE_URL . ltrim($consola->imagen, './') ?>" class="card-img-top" alt="<?= htmlspecialchars($consola->nombre) ?>" style="height:100px; object-fit:cover;">
                    <?php else: ?>
                        <div class="card-img-top bg-secondary text-white d-flex align-items-center justify-content-center" style="height:100px; font-size:0.8rem;">
                            Sin Imagen
                        </div>
                    <?php endif; ?>

                    <div class="card-body p-2">
                        <!-- Nombre -->
                          <!-- Nombre y link -->
                        <h5 class="card-title">
                            <a href="<?= BASE_URL ?>juegos-categoria/<?= $consola->id ?>"><?= htmlspecialchars($consola->nombre) ?></a>
                        </h5>
                       
                        <!-- Empresa -->   <p class="card-text mb-1"> #<?= htmlspecialchars($consola->id) ?></p>
                        <p class="card-text mb-1"><?= htmlspecialchars($consola->empresa) ?></p>

                        <?php if (isset($_SESSION['ID_USER'])): ?>
                            <div class="d-flex justify-content-center gap-1 mt-1">
                                <a href="<?= BASE_URL . 'editar-consola/' . $consola->id ?>" class="btn btn-primary btn-sm">Editar</a>
                                <a href="<?= BASE_URL . 'eliminar-consola/' . $consola->id ?>" class="btn btn-danger btn-sm">Eliminar</a>
                            </div>
                        <?php endif; ?>
                    </div>
                </section>
            </div>
                        </a>
        
    </div>
</div>


