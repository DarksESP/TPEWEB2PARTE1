

<?php 
if (session_status() === PHP_SESSION_NONE) {
    session_start();
}
 if (isset($_SESSION['ID_USER'])) : ?>
    <div class="container mb-4 d-flex justify-content-end">
        <a href="<?= BASE_URL ?>agregar-juego" class="btn btn-warning btn-lg shadow-sm">
            <i class="bi bi-plus-circle me-2"></i> Agregar Juego
        </a>
    </div>

    <?php endif; ?>