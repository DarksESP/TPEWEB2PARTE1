

<section class="container py-4">
    <div class="row g-4">
        <?php foreach ($juegos as $juego) : ?>
            <div class="col-md-4 col-lg-3">
                <div class="card h-100 shadow-sm">
                    <!-- Imagen del juego -->
                    <?php if (!empty($juego->imagen)) : ?>
                        <img src="<?= BASE_URL . ltrim($juego->imagen, './') ?>" class="card-img-top" alt="<?= htmlspecialchars($juego->nombre) ?>">
                    <?php else : ?>
                        <img src="<?= BASE_URL ?>img/default-game.png" class="card-img-top" alt="Sin imagen">
                    <?php endif; ?>

                    <div class="card-body d-flex flex-column">
                        <!-- Nombre y link -->
                        <h5 class="card-title">
                            <a href="<?= BASE_URL ?>juegos/<?= $juego->id ?>"><?= htmlspecialchars($juego->nombre) ?></a>
                        </h5>

                          <p class="card-text mb-1"><strong>#</strong> <?= htmlspecialchars($juego->id) ?></p>
                        <!-- Género y consola -->
                        <p class="card-text mb-1"><strong>Género:</strong> <?= htmlspecialchars($juego->genero) ?></p>
                        <p class="card-text mb-2">
                            <strong>Consola:</strong>
                            <?php
                            foreach ($consolas as $consola) {
                                if ($consola->id == $juego->id_consola) {
                                    echo htmlspecialchars($consola->nombre);
                                }
                            }
                            ?>
                        </p>

                        <!-- Botones si hay sesión -->
                        <?php if (isset($_SESSION["ID_USER"])) : ?>
                            <div class="mt-auto d-flex justify-content-between">
                                <a href="<?= BASE_URL ?>editar-juego/<?= $juego->id ?>" class="btn btn-sm btn-primary">Editar</a>
                                <a href="<?= BASE_URL ?>eliminar-juego/<?= $juego->id ?>" class="btn btn-sm btn-danger">Eliminar</a>
                            </div>
                        <?php endif; ?>
                    </div>
                </div>
            </div>
        <?php endforeach; ?>
    </div>
</section>
<?php
